// import s from './UserItem.module.css'

// let UserItem = (props) => {

    // let id = props.id;

    // let follow = (props) =>{
    //     props.follow(id)
    // }

    // let follow = (props) =>{
    //     props.follow(id)
    // }

    // return (
    //     <div className={s.user_item}>
    //         <div className={s.img_btn}>
    //             <div className={s.user_img}><img src={props.img} alt="" /> </div>
    //             {props.followed ? <div className={s.follow_btn}>follow</div> : <div className={s.follow_btn}>unfollow</div>}

    //         </div>
    //         <div className={s.user_info}>
    //             <div className={s.name}>{props.userName}, {props.UserAge}</div>
    //             <div className={s.location}> {props.city}, {props.country} </div>
    //             <div className={s.status}> {props.status} </div>
    //         </div>
    //     </div>
    // )
// }

// let UserItem = (props) => {
//     return (
//         <div className={s.user_item}>
//             <div className={s.img_btn}>
//                 <div className={s.user_img}><img src={props.users.img} alt="" /> </div>
//                 {props.followed ? <div onClick={props.users.follow} className={s.follow_btn}>follow</div> : <div className={s.follow_btn}>unfollow</div> }

//             </div>
//             <div className={s.user_info}>
//                 <div className={s.name}>{props.users.userName}, {props.users.UserAge}</div>
//                 <div className={s.location}> {props.users.city}, {props.users.country} </div>
//                 <div className={s.status}> {props.users.status} </div>
//             </div>
//         </div>
//     )
// }

// export default UserItem;